<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Locked obsidian background (#05070B) — used for the native splash
         window so there is no white flash before Flutter's first frame. -->
    <color name="splash_background">#FF05070B</color>

    <!-- Adaptive-icon background layer — matches the new "noor"
         wordmark artwork's own navy gradient edge tone (2026-09-05,
         direct request: user-supplied icon), not the app's locked
         obsidian, so the mask-cropped area blends with the artwork
         instead of showing a mismatched ring around it. -->
    <color name="ic_launcher_background">#FF1E2F43</color>
</resources>
