// Bismillahir Rahmanir Raheem — watermark: ALLAH
//
// Original line-art icon set for the bottom nav and the More screen
// (2026-08-27, direct request: "design original icons... in noor's
// own locked gold/cyan palette, not copied from anywhere" — the Athan
// app was named only as a reference for polish level, never for the
// actual glyphs). Replaces plain Material icons that read as generic
// stock-icon-set glyphs with shapes drawn for this app specifically —
// a sundial for Prayer Times instead of a wall clock, prayer beads
// for Tasbih instead of a generic circular-blur glyph, a balance
// scale for Zakat instead of a calculator. See noor_icon_painters_*
// for the actual path data.
enum NoorIconType {
  home,
  prayerTimes,
  quran,
  duas,
  more,
  qibla,
  tasbih,
  calendar,
  zakat,
  settings,
  about,
  feedback,
}
