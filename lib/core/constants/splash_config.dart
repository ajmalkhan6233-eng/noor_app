// Bismillahir Rahmanir Raheem — watermark: ALLAH
//
// Tunables for the launch splash: a calm, still greeting on paper —
// no animated star field, no dark cosmic sequence.

abstract final class SplashConfig {
  /// How long the greeting holds before fading into the dashboard.
  static const Duration holdDuration = Duration(milliseconds: 1800);

  /// Fade-in and fade-out duration.
  static const Duration fadeDuration = Duration(milliseconds: 500);
}
